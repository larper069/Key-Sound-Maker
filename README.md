# Key-Sound-Maker
just a python script that outputs different sounds when you press different keys
 You will need some dependencies first #pygame and #pynput you can install them using pip
# The keys are related like this if you want to change them
•space uses space.wav
•enter uses enter.wav
•mod uses  mod.wav
•Num uses  num.wav

